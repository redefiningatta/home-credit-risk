name: 'dbt_home_credit'
version: '1.0.0'
config-version: 2

profile: 'dbt_home_credit'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_home_credit:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: table
      +schema: intermediate
    marts:
      credit_risk:
        +materialized: table
        +schema: marts
      finance:
        +materialized: table
        +schema: marts
